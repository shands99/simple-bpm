package org.jemco.simplebpm.runtime.components;

import org.jemco.simplebpm.runtime.WorkflowSession;

public class FlowNodeProcessor implements NodeProcessor {

	@Override
	public void handle(WorkflowSession session) throws Exception {
		
		// create the child execution states that match the branches
		
		
		// check if there is a sync point, if so create the token and activate.
		
		// else we 
		
	}

}
